$kind: http-request
name: Get Dictados by Curso y Materia
method: GET
url: '{{baseUrl}}/api/dictados/curso/:cursoId/materia/:materiaId'
order: 3000
pathVariables:
  - key: cursoId
    value: '1'
  - key: materiaId
    value: '1'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200", function () {
        pm.response.to.have.status(200);
      });
      pm.test("Response is an array", function () {
        const json = pm.response.json();
        pm.expect(json).to.be.an('array');
      });
