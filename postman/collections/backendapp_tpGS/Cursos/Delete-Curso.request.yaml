$kind: http-request
name: Delete Curso
method: DELETE
url: '{{baseUrl}}/api/cursos/1'
order: 5000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200, 204 or 404", function () {
        pm.expect(pm.response.code).to.be.oneOf([200, 204, 404]);
      });
