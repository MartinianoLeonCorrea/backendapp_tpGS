const FECHA_EXAMEN = {
  required: true,
};

const TEMAS = {
  min: 5,
  max: 800,
  required: true,
};

const COPIAS = {
  min: 1,
  required: true,
};

const DICTADO_ID = {
  required: true,
};

module.exports = {
  FECHA_EXAMEN,
  TEMAS,
  COPIAS,
  DICTADO_ID,
};
