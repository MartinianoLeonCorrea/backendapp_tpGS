const NOTA = {
  min: 1,
  max: 10,
  required: true,
};

const OBSERVACIONES = {
  min: 0,
  max: 500,
  required: false,
};

const ALUMNO_ID = {
  required: true,
};

const EXAMEN_ID = {
  required: true,
};

module.exports = {
  NOTA,
  OBSERVACIONES,
  ALUMNO_ID,
  EXAMEN_ID,
};
