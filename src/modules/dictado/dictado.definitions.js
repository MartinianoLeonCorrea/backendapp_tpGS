const DICTADO_ANIO = {
  MIN: 2000,
  MAX: 2100,
  REQUIRED: true,
};

const DICTADO_DIAS_CURSADO = {
  MIN: 3,
  MAX: 100,
  REQUIRED: true,
};

const DICTADO_CURSO_ID = {
  MIN: 1,
  REQUIRED: true,
};

const DICTADO_MATERIA_ID = {
  MIN: 1,
  REQUIRED: true,
};

const DICTADO_DOCENTE_ID = {
  MIN: 1,
  REQUIRED: true,
};

const DICTADO_ID = {
  MIN: 1,
  REQUIRED: true,
};

const IDS_ARRAY = {
  MIN_ITEMS: 1,
  REQUIRED: false,
};

module.exports = {
  DICTADO_ANIO,
  DICTADO_DIAS_CURSADO,
  DICTADO_CURSO_ID,
  DICTADO_MATERIA_ID,
  DICTADO_DOCENTE_ID,
  DICTADO_ID,
  IDS_ARRAY,
};